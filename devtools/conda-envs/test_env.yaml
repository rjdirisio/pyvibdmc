name: test
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Package dependencies
   - numpy
   # - matplotlib #will be necessary in the future
   - h5py

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

