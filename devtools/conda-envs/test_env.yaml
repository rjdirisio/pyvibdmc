name: test
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Package dependencies
  - numpy
  - h5py
  - matplotlib
  - sphinx-autoapi

    # Testing
  - pytest
  - pytest-cov
  - codecov
