name: test
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Package dependencies
  - numpy
  - h5py
  - matplotlib
  - tensorflow

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip installs
  - pip:
      - tensorflow